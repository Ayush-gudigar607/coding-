<!-- Install TypeScript as a development dependency: -->
npm install typescript --save-dev

<!-- Generate the tsconfig.json file: -->
npx tsc --init

<!-- 
The command npx tsc is used to invoke the TypeScript Compiler (tsc) to transpile TypeScript code into JavaScript.  -->
 npx tsc

